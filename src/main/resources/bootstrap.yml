server:
  port: 8080
security:
  basic:
    enabled: false

eureka:
  client:
    registerWithEureka: true
    fetchRegistry: true
  server:
    waitTimeInMsWhenSyncEmpty: 0

spring:
  application:
    name: proto-infrastructure-gateway